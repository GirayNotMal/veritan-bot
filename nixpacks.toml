# Railway (Nixpacks) icin: FFmpeg ve libopus sistem paketlerini kurar.
# Ses calma/dinleme bunlar olmadan CALISMAZ.
[phases.setup]
nixPkgs = ["python311", "ffmpeg", "libopus"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python veritan_bot.py"
